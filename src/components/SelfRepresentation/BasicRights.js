
import React from 'react';
import { Card, CardContent, Typography } from '@mui/material';

function BasicRights() {
  return (
    <Card style={{ marginBottom: '20px' }}>
      <CardContent>
        <Typography variant="h5">Basic Legal Principles and Rights</Typography>
        <Typography variant="body2">
          Everyone has the right to a fair trial and legal representation. 
          Here’s a brief overview of key rights.
        </Typography>
      </CardContent>
    </Card>
  );
}

export default BasicRights;
